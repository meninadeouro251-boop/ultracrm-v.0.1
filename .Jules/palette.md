## 2025-05-22 - [Loading States and Accessibility in Base Components]
**Learning:** Shared base components like `BaseTable` and `BaseForm` are high-leverage points for UX improvements. Adding animated loaders and ARIA labels here benefits the entire application.
**Action:** Always check base components for missing feedback (loading states) and accessibility (ARIA labels) as they propagate to multiple features.
